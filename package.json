{
  "name": "spotify-copycat",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://ahmed-wagdi.github.io/spotify-copycat",
  "devDependencies": {
    "gh-pages": "^1.0.0",
    "node-sass": "^4.5.0",
    "react-scripts": "0.9.5",
    "sass-loader": "^6.0.3"
  },
  "dependencies": {
    "axios": "^0.15.3",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-player": "^0.14.3",
    "react-redux": "^5.0.4",
    "react-router-dom": "^4.0.0",
    "redux": "^3.6.0",
    "redux-thunk": "^2.2.0"
  },
  "scripts": {
    "build-css": "node-sass src/ -o src/",
    "watch-css": "npm run build-css && node-sass src/ -o src/ --watch --recursive",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "npm run build&&gh-pages -d build"
  }
}
